function max(a,b,c)
{
    r=Math.max(a,b,c)
    result.innerText=r
}
function min(a,b,c)
{
    r=Math.min(a,b,c)
    result.innerText=r
}
function sin(a)
{
    r=Math.sin(a)
    result.innerText=r
}
function cos(a)
{
    r=Math.cos(a)
    result.innerText=r
}
function mu(a,b)
{
    r=Math.pow(a,b)
    result.innerText=r
}